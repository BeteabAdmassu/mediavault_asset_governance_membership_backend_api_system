{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "auth_per_hour": {"type": "integer", "minimum": 1},
    "read_per_minute": {"type": "integer", "minimum": 1},
    "write_per_minute": {"type": "integer", "minimum": 1}
  },
  "required": ["auth_per_hour"]
}
