{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "max_courses_per_user": {"type": "integer", "minimum": 1},
    "enrollment_window_days": {"type": "integer", "minimum": 1},
    "waitlist_enabled": {"type": "boolean"}
  },
  "required": ["max_courses_per_user"]
}
